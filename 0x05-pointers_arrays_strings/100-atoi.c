#include "main.h"

/**
 * _atoi - convert a string to an integer
 * @s: pointer to convert
 * Return: an integer
 */

int _atoi(char *s)
{
	int c = 0;
	int min = 1;

	while (s[c])
	{
	if (s[c] == 45)
	{
	min *= -1;
	}
	while (s[c] >= 48 && s[c] <= 57)
	}
}
