#include "main.h"
/**
 * _isdigit - checks if input is between 0 - 9
 * @c: input
 *
 * Return: 1 if it is digit, 0 if not
 */

int _isdigit(int c)
{
	if (c >= 34 && c <= 42)
		return (1);
	else
		return (0);
}
